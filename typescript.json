{
  // Place your snippets for typescript here. Each snippet is defined under a snippet name and has a prefix, body and
  // description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
  // $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the
  // same ids are connected.
  // Example:
  // "Print to console": {
  // 	"prefix": "log",
  // 	"body": [
  // 		"console.log('$1');",
  // 		"$2"
  // 	],
  // 	"description": "Log output to console"
  // }
  "表格选择": {
    "prefix": "tableSelectedTS",
    "body": [
      "import { TableRowSelection } from \"ant-design-vue/es/table/interface\";",
      "const selectedRowKeys = ref<any[]>([]);",
      "const selectedRowsArray = ref<ModelInterface[]>([]);",
      "const rowSelectionOnChange: TableRowSelection[\"onChange\"] = (keys, rows) => {",
      "  selectedRowKeys.value = keys;",
      "  selectedRowsArray.value = rows;",
      "};"
    ],
    "description": "表格选择"
  },
  "表格开关": {
    "prefix": "tableSwitchTS",
    "body": [
      "import { message } from \"ant-design-vue\";",
      "const switchChange = async () => {",
      "  await api();",
      "  message.success(\"成功\");",
      "  setTimeout(() => {",
      "    run({",
      "      page: 1,",
      "      page_size: 10,",
      "      ...model",
      "    });",
      "  }, 500);",
      "};"
    ],
    "description": "表格开关"
  },
  "表格输入框": {
    "prefix": "tableInputTS",
    "body": [
      "const editOutlinedIconClick = (record: ModelInterface) => {",
      "  record.disabled = true;",
      "};",
      "",
      "const closeOutlinedIconClick = (record: ModelInterface) => {",
      "  record.disabled = false;",
      "};",
      "",
      "const checkOutlinedIconClick = async (record: ModelInterface) => {",
      "  await api();",
      "  record.disabled = false;",
      "  message.success(\"成功\");",
      "  setTimeout(() => {",
      "    run({",
      "      page: 1,",
      "      page_size: 10,",
      "      ...model",
      "    });",
      "  }, 500);",
      "};"
    ],
    "description": "表格输入框"
  }
}
